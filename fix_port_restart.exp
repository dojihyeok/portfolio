#!/usr/bin/expect
set timeout 60
spawn ssh -o StrictHostKeyChecking=no root@49.50.139.88
expect "*?assword:*"
send "R4+r525MP5DiBi\r"
expect "*?root@*"
# Find PID using port 3003 and kill it (excluding the current shell if any, though lsof/netstat safer)
send "lsof -i :3003 -t | xargs kill -9\r"
expect "*?root@*"
send "pm2 restart portfolio\r"
expect "*?root@*"
send "exit\r"
expect eof
