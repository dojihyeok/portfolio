#!/usr/bin/expect -f
set timeout -1
spawn ssh -i /Users/yunhyeok/triedit-dev.pem -o StrictHostKeyChecking=no root@49.50.139.88
expect "password:"
send "R4+r525MP5DiBi\r"
expect "#"
send "cd /root/portfolio && git clean -fd && git reset --hard && git pull origin main\r"
expect "#"
send "rm -rf .next && npm run build\r"
expect "#"
send "pm2 restart portfolio\r"
expect "#"
send "exit\r"
